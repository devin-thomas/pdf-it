# Optional local-only defaults. The app normally asks for a key per session.
# Never commit real API keys. Copy this file to .env only for local tooling.
GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
